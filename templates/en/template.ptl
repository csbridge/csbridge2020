<!--
	A template used for every English page - contains the nav bar, a container div, and the footer.
-->
% rebase('templates/template.ptl', title=title, taMode=get('taMode', False))

<!-- Navigation Bar -->
% include('templates/parts/navBar.ptl', lang="en", taMode=get('taMode', False))

<div class="container">
	
	{{!base}}
			
	<!-- Footer -->
	% include('templates/en/footer.ptl', taMode=get('taMode', False))

</div>
