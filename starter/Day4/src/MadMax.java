import acm.graphics.*;
import acm.program.*;
import acm.util.RandomGenerator;
import java.awt.*;

public class MadMax extends ConsoleProgram {

	public void run() {
	    println("The max of 4 and 5 is: " + max(4, 5));
	    println("The max of -4 and 4 is: " + max(-4, 4));
	    println("The max of 1 and 2 is: " + max(1, 2));
	    println("The max of 0 and 0 is: " + max(0, 0));
	}

	/** 
	 * Method: Max
	 * -----------
	 * The max method takes two inputs (i and j) and returns
	 * the larger of the two.
	 */
	private int max(int i, int j){
		// your code here...
		return 0;
	}
}